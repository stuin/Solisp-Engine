#pragma once

enum CollisionLayers {
	BACKGROUND,
	STACKS,
	DISPLAY,
	ACTIONS,
	POINTER,
	FADE,
	MENU,
	TEXT,
	INPUT,
	CAMERA
};

enum SubMenus {
	MAINMENU,
	PAUSEMENU,
	ACTIONMENU,
	STARTMENU,
	LOADMENU,
	CARDMENU1,
	CARDMENU2
};

enum Signals {
	STARTGAME,
	RELOADCARDS,
	WINGAME,
	ENDGAME
};

enum MenuDirections {
	UP,
	LEFT,
	DOWN,
	RIGHT
};